<h2>Character <%= link_to "(+)", new_house_character_path %></h2>

<ul>
  <% @characters.each do |character| %>
    <li>
      <%= link_to character.name, house_character_path(character) %>
    </li>
  <% end %>
</ul>
